import { JobsBrowser } from '@/components/career-match/JobsBrowser';

export default function CareerJobsPage() {
  return (
    <JobsBrowser
      title="Job Search"
      description="Browse live startup roles, filter for the right fit, and save the opportunities you want to revisit."
    />
  );
}
